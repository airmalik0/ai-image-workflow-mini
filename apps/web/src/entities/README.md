# entities

Доменные сущности приложения: `workflow`, `node`, `preset`, `run`, `job` — модель и
презентационные компоненты. Заполняется задачами 17–20.

Импортировать отсюда можно только `shared` и соседние файлы своего же среза.
