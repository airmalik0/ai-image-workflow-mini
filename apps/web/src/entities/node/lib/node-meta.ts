import type { NodeKind } from '@workflow/contracts'

/** Русские названия типов нод. Порты и параметры берутся из `NODE_SPECS`, здесь только подписи. */
export const NODE_LABELS: Record<NodeKind, string> = {
  prompt: 'Промпт',
  imageInput: 'Изображение',
  generateImage: 'Генерация',
  editImage: 'Редактирование',
  result: 'Результат',
}

/**
 * Подписи параметров. Только оформление: список полей инспектор берёт из
 * `NODE_SPECS`, а незнакомый параметр покажет под собственным именем — новое
 * поле в контракте не исчезнет из формы только потому, что здесь его забыли.
 */
export const PARAM_LABELS: Record<string, string> = {
  text: 'Текст промпта',
  instruction: 'Инструкция',
  fileId: 'Изображение',
  presetId: 'Пресет',
  model: 'Модель',
  aspectRatio: 'Пропорции кадра',
}

export const PARAM_HINTS: Record<string, string> = {
  text: 'Уходит на выход text — его принимает вход prompt ноды генерации',
  instruction: 'Что изменить в исходном изображении',
  presetId: 'Главный промпт и референсы пресета подмешиваются к запросу',
  model: 'Пусто — провайдер возьмёт свою модель по умолчанию',
}

/**
 * Пропорции кадра. Схема принимает любую строку, поэтому набор — решение
 * интерфейса: пять привычных кадров вместо поля, куда можно вписать что угодно.
 */
export const ASPECT_RATIOS = ['1:1', '4:3', '3:4', '16:9', '9:16'] as const

/** Строка под названием в палитре: что нода делает, одним предложением. */
export const NODE_DESCRIPTIONS: Record<NodeKind, string> = {
  prompt: 'Текст задания для генерации',
  imageInput: 'Исходный файл для редактирования',
  generateImage: 'Создаёт изображение по тексту',
  editImage: 'Меняет изображение по инструкции',
  result: 'Показывает готовое изображение',
}
