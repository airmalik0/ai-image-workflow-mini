# features

Пользовательские сценарии: `add-node`, `connect-nodes`, `select-preset`, `select-model`,
`upload-image`, `run-workflow`, `retry-job`, `cancel-run`, `load-scenario`.
Заполняется задачами 18–22.

Импортировать отсюда можно `entities` и `shared`.
