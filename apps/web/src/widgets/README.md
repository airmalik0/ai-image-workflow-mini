# widgets

Композиции: `workflow-canvas`, `node-palette`, `node-inspector`, `run-timeline`, `presets-panel`.
Заполняется задачами 18–21.

Виджеты только рисуют. Бизнес-логика живёт в `features`, `entities` и `@workflow/core`.
